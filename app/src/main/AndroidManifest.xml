<?xml version="1.0" encoding="utf-8"?>
<manifest package="pillar_technology.vendingmachine"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/VendingTheme">

        <activity
            android:name=".VendingMachineActivity"
            android:configChanges="screenSize|layoutDirection"
            android:launchMode="singleTop"
            android:theme="@style/VendingTheme.ActionBarStyle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>